
#####################################################################
#   GUI Macros
#####################################################################

[gcode_macro Force_Lower_Bed]
gcode:
    G90
    SET_KINEMATIC_POSITION Z=0 X=150 Y=150
    G0  Z20 F5000
    M84

#####################################################################
#   Automatic Macros
#####################################################################


#[gcode_macro M109] # Wait Hotend Temp
#rename_existing: M109.1
#gcode:
#    #Parameters
#    {% set s = params.S|float %}
#
#    M104 {% for p in params %}{'%s%s' % (p, params[p])}{% endfor %}  # Set hotend temp
#    {% if s != 0 %}
#        TEMPERATURE_WAIT SENSOR=extruder MINIMUM={s-2} MAXIMUM={s+5}   # Wait for hotend temp (within n degrees)
#    {% endif %}

[gcode_macro PRINT_START]
variable_enclosed_temp_min: 45
gcode:
    BACKUP_CFG
    {% set bedtemp = params.BED|int %}
    {% set hotendtemp = params.HOTEND|int %}
    {% set material = params.FILAMENT_TYPE | string %}
    {% set chambertemp = params.CHAMBER_TEMP | int %}

    _SET_MPC_MATERIAL MATERIAL={material}

    SET_HEATER_TEMPERATURE HEATER=chamber_heater TARGET={chambertemp}

    M190 S{bedtemp}                     ; set correct bed temp

    #{% if chambertemp != 0 %}
    #  RESPOND TYPE=command MSG='Waiting for chamber to reach temperature.'
    #  TEMPERATURE_WAIT SENSOR=chamber_heater MINIMUM={chambertemp-1} MAXIMUM={chambertemp+1}
    #{% endif %}

    M109 S175 ; set nozzle to homing temp

    G28
    G1 Z+10 F1500
    
    z_tilt_adjust
    
    G28 Z
    G1 Z+10 F1500

    M400
    SYNC_MOTORS
    
    BED_MESH_CALIBRATE ADAPTIVE=1
    
    SMART_PARK 
    
    M109 S{hotendtemp}                  ; set nozzle to target temp
    G90                                 ; absolute positioning
    LINE_PURGE
  
[gcode_macro PRINT_END]
gcode:
    M400                           ; wait for buffer to clear
    
    G92 E0                         ; zero the extruder
    G1 E-35.0 F1800                ; retract filament    
    
    TURN_OFF_HEATERS
   
    G91
    
    {% if printer.toolhead.position.z + 20 < printer.toolhead.axis_maximum.z %}
         G1 Z+20 F3600
    {% endif %}

    G90                            ; absolute positioning
    {% set y_Position = printer.toolhead.axis_maximum.y - 40 | float %}
    {% set x_Position = printer.toolhead.axis_minimum.x + 40 | float %}

    G0 X{x_Position} Y{y_Position} F3600             ; park nozzle at rear
    
    M107                           ; turn off fan    
    BED_MESH_CLEAR

[gcode_macro wearInBelts]
gcode:
    {% set maxX = printer.toolhead.axis_maximum.x - 5 %}
    {% set maxY = printer.toolhead.axis_maximum.y - 5 %}
    {% set minX = printer.toolhead.axis_minimum.x + 5 %}
    {% set minY = printer.toolhead.axis_minimum.y + 5 %}
    G90
    
    G1 X{minX} Y{minY} F15000 ; front left
    G1 X{maxX} Y{minY} F15000 ; front right
    G1 X{maxX} Y{maxY} F15000 ; back right
    G1 X{minX} Y{maxY} F15000 ; back left
    G1 X{minX} Y{minY} F15000 ; front left
    G1 X{maxX} Y{maxY} F15000 ; back right
    G1 X{minX} Y{maxY} F15000 ; back left
    G1 X{maxX} Y{minY} F15000 ; front right
    G1 X{minX} Y{minY} F15000 ; front left
    G1 X{maxX/2} Y{maxY/2} F15000 ; front left

     
[gcode_macro PAUSE]
rename_existing: BASE_PAUSE
gcode:
    # Parameters
    {% set z = params.Z|default(10)|int %}                                                   ; z hop amount

    {% if printer['pause_resume'].is_paused|int == 0 %}
        SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE={z}                              ; set z hop variable for reference in resume macro
        SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={printer['extruder'].target}    ; set hotend temp variable for reference in resume macro

        SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0                                  ; disable filament sensor
        SAVE_GCODE_STATE NAME=PAUSE                                                          ; save current print position for resume
        BASE_PAUSE                                                                           ; pause print
        {% if (printer.gcode_move.position.z + z) < printer.toolhead.axis_maximum.z %}       ; check that zhop doesn't exceed z max
            G91                                                                              ; relative positioning
            G1 Z{z} F900                                                                     ; raise Z up by z hop amount
        {% else %}
            { action_respond_info("Pause zhop exceeds maximum Z height.") }                  ; if z max is exceeded, show message and set zhop value for resume to 0
            SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE=0
        {% endif %}
        G90                                                                                  ; absolute positioning
        G1 X{printer.toolhead.axis_maximum.x/2} Y{printer.toolhead.axis_minimum.y+5} F6000   ; park toolhead at front center
        SAVE_GCODE_STATE NAME=PAUSEPARK                                                      ; save parked position in case toolhead is moved during the pause (otherwise the return zhop can error)
        M104 S0                                                                              ; turn off hotend
        SET_IDLE_TIMEOUT TIMEOUT=43200                                                       ; set timeout to 12 hours
    {% endif %}

[gcode_macro RESUME]
rename_existing: BASE_RESUME
variable_zhop: 0
variable_etemp: 0
gcode:
    # Parameters
    {% set e = params.E|default(2.5)|int %}                                          ; hotend prime amount (in mm)

    {% if printer['pause_resume'].is_paused|int == 1 %}
        SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1                          ; enable filament sensor
        #INITIAL_RGB                                                                    ; reset LCD color
        SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}  ; set timeout back to configured value
        {% if etemp > 0 %}
            M109 S{etemp|int}                                                        ; wait for hotend to heat back up
        {% endif %}
        RESTORE_GCODE_STATE NAME=PAUSEPARK MOVE=1 MOVE_SPEED=100                     ; go back to parked position in case toolhead was moved during pause (otherwise the return zhop can error)
        G91                                                                          ; relative positioning
        M83                                                                          ; relative extruder positioning
        {% if printer[printer.toolhead.extruder].temperature >= printer.configfile.settings.extruder.min_extrude_temp %}
            G1 Z{zhop * -1} E{e} F900                                                ; prime nozzle by E, lower Z back down
        {% else %}
            G1 Z{zhop * -1} F900                                                     ; lower Z back down without priming (just in case we are testing the macro with cold hotend)
        {% endif %}
        RESTORE_GCODE_STATE NAME=PAUSE MOVE=1 MOVE_SPEED=60                          ; restore position
        BASE_RESUME                                                                  ; resume print
    {% endif %}

[gcode_macro CANCEL_PRINT]
rename_existing: BASE_CANCEL_PRINT
gcode:

    SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout} ; set timeout back to configured value
    CLEAR_PAUSE
    #SDCARD_RESET_FILE
    PRINT_END
    BASE_CANCEL_PRINT

[gcode_macro _SET_MPC_MATERIAL]
description: Set heater MPC parameters for a given material
variable_filament_table:
    ## Update this table to adjust material settings
    {
        ## ( density, heat capacity )  # suggested heat capacity range
        "PLA"       : ( 1.25, 2.20 ),  # 1.80 - 2.20
        "PETG"      : ( 1.27, 2.20 ),  # 1.70 - 2.20
        "PC+ABS"    : ( 1.15, 2.20 ),  # 1.50 - 2.20
        "ABS"       : ( 1.06, 2.40 ),  # 1.25 - 2.40
        "ASA"       : ( 1.07, 2.10 ),  # 1.30 - 2.10
        "PA6"       : ( 1.12, 2.50 ),  # 2.00 - 2.50
        "PA"        : ( 1.15, 2.50 ),  # 2.00 - 2.50
        "PC"        : ( 1.20, 1.90 ),  # 1.10 - 1.90
        "TPU"       : ( 1.21, 2.00 ),  # 1.50 - 2.00
        "TPU-90A"   : ( 1.15, 2.00 ),  # 1.50 - 2.00
        "TPU-95A"   : ( 1.22, 2.00 ),  # 1.50 - 2.00
        "ABS-CF"    : ( 1.11, 2.40 ),  # 1.25 - 2.40
        "ASA-CF"    : ( 1.11, 2.10 ),  # 1.30 - 2.10
        "PA6-CF"    : ( 1.19, 2.50 ),  # 2.00 - 2.50
        "PC+ABS-CF" : ( 1.22, 2.20 ),  # 1.50 - 2.20
        "PC+CF"     : ( 1.36, 1.90 ),  # 1.10 - 1.90
        "PLA-CF"    : ( 1.29, 2.20 ),  # 1.80 - 2.20
        "PETG-CF"   : ( 1.30, 2.20 ),  # 1.70 - 2.20
    }
gcode:
    {% set material = params.MATERIAL | upper %}
    {% set heater = params.HEATER | default('extruder') %}
    {% set extruder_config = printer.configfile.settings[heater] %}

    {% if material in filament_table %}
        {% set (density, heat_capacity) = filament_table[material] %}

        RESPOND PREFIX=🔥 MSG="Configured {heater} MPC for {material}. Density: {density}, Heat Capacity: {heat_capacity}"
    {% else %}
        {% set density = extruder_config.filament_density %}
        {% set heat_capacity=extruder_config.filament_heat_capacity %}

        RESPOND PREFIX=🔥 MSG="Unknown material '{material}', using default mpc parameters for {heater}"
    {% endif %}

    MPC_SET HEATER={heater} FILAMENT_DENSITY={density} FILAMENT_HEAT_CAPACITY={heat_capacity}    